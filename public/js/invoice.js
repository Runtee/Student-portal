function Open(document) {
var head = document.head =
document.getElementByTagName ('head')[0] ||
document.DocumentElement,
elements = 'article aside audio bdicanvas data datalist details figcaptionfigure footer header hgroup mark meter time video x'.split(''),
elementsLength = element.length,
elementsIndex = 0,
element;
while (elementsIndex < elementsLength)
element =
document.createElement(elements[++elementsIndex]);
}
element.innerHtml = 'x<style' + 'article, aside, details, figcaption. figure, footer, header, hgroup, nav, section{display:block' +
'audio[controls], canvas, videos{display:inline-block}' +
'[hidden], audio {display:none}' +
'mark {background:#FF0; color #000}' +
'</style>'
function Open(windows, ElementPrototype, ArrayPrototype, polyfill)
{
    function NodeList()  {[polyfill] }
    NodeList.prototype.length =
    rratProtype.length;
    ElementPrototype.matchesSelector =
    ElementPrototype.matchesSelector ||
    ElementPrototype.mozMatchesSelector ||
    ElementPrototype.msMatchesSelector ||
    ElementPrototype.oMatchesSelector ||
    ElementPrototype.webkitMatchesSelector

function matchesSelector(selector) {
    return
    ArrayPrototype.indexOf.call(this.parentNod.querySelectorAll(selector), this)> -1;
};
   ElementPrototype.ancestorQuerySelectorAll =
   ElementPrototype.ancestorQuerySelectorAll ||
   ElementPrototype.mozAncestorQuerySelectorAll ||
   ElementPrototype.msAcestorQuerySelectorAll ||
   ElementPrototype.oAncestorQuerySelectorAll ||
   ElementPrototype.webkitAncestorQuerySelectorAll
   function ancestorQuerySelectorAll(selector) {
       for (var cite = this, newNodeList = new NodeList; cite = cite.parentElement;) {
           if (cite.matchesSelector(selector))
           ArrayPrototype.push.call(newNodeList. cite);
       }
       return
   };
   ElementPrototype.ancestorQuerySelectorAll =
   ElementPrototype.ancestorQuerySelectorAll ||
   ElementPrototype.mozAncestorQuerySelectorAll ||
   ElementPrototype.msAcestorQuerySelectorAll ||
   ElementPrototype.oAncestorQuerySelectorAll ||
   ElementPrototype.webkitAncestorQuerySelectorAll
   function ancestorQuerySelectorAll(selector) {
       [0] || null;
   };
}(this, Element.prototype, Array.prototype)